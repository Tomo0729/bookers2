
    <div class="col-md-3">
     <% if book.errors.any? %>
      <div class="error">
       <h2><%= book.errors.count %>error prohibited this obj from being saved:</h2>
       <ul>
        <% book.errors.full_messages.each do |message| %>
         <li><%= message %></li>
        <% end %>
       </ul>
      </div>
　   <% end %>

     <h2>User info</h2>
     <%= image_tag users.get_profile_image(150,150) %>
     <table class="table">
      <tbody>
       <tr>
        <th>name</th>
        <th><%= users.name %></th>
       </tr>
       <tr>
        <th>introduction</th>
        <th><%= users.introduction %></th>
       </tr>
      </tbody>
     </table>

       <%= link_to edit_user_path(users), class:"btn btn-outline-secondary btn-block" do %>
        <span><i class="fas fa-user-cog"></i></span>
       <% end %>


     <h2>New book</h2>
     <%= form_with model: book do |f| %>
      <div class="form-group">
       <h6>Title</h6>
       <%= f.text_field :title, class: "form-control book_title col-md-12" %>
      </div>
      <div class="form-group">
       <h6>Opinion</h6>
       <%= f.text_area :body, class: "form-control book_body col-md-12" %>
      </div>
       <%= f.submit 'Create Book', class: "btn btn-success col-xs-12" %>
     <% end %>
    </div>
